{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "recursion",
      "type": "cppdbg",
      "program": "${workspaceFolder}/recursion",
      "request": "launch",
      "cwd": "${workspaceFolder}"
    }
  ]
}